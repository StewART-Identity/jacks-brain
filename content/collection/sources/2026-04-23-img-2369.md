---
title: "SAML vs. OAuth Diagram"
summary: "Side-by-side instructional diagram comparing the request flow of SAML and OAuth, captured as a photo."
type: source
created: 2026-04-23
updated: 2026-04-23
tags:
  - saml
  - oauth
  - identity
  - authentication
  - authorization
  - iam
sources: []
confidence: high
role: reference
views:
  - date: 2026-04-23
    note: "Initial cataloging."
---

A hand-drawn or printed instructional diagram from "Reco" comparing the flow of [[collection/concepts/saml|SAML]] and [[collection/concepts/oauth|OAuth]] side by side. Captured as a photo on 2026-04-23.

[Download original](/originals/2026-04-23-IMG_2369.png)

## SAML Flow

The diagram depicts a seven-step SAML assertion flow:

1. **User requests access** to a resource hosted by the Service Provider.
2. **Unauthenticated requests are redirected** — the Service Provider redirects the user to the SAML Identity Provider (IdP) with a SAML request.
3. **Display login page** — the IdP presents a login screen to the user.
4. **Credentials sent for verification** — credentials are checked against an [[collection/entities/active-directory|Active Directory]] / User Data Base.
5. **Sends verification status** — the directory returns a pass/fail to the IdP.
6. **IdP sends SAML response** — the Identity Provider issues a SAML assertion (XML token) back to the Service Provider.
7. **User receives access to the resource** — the Service Provider grants access based on the assertion.

**Key components:** User, Login Screen, [[collection/entities/active-directory|Active Directory]]/User DB, SAML Identity Provider, Service Provider.

## OAuth Flow

The diagram depicts a seven-step OAuth authorization flow:

1. **User accesses resource** — the Resource Owner attempts to reach a protected resource.
2. **Client requests authorization** — usually via a login page, the Client asks for permission.
3. **User inputs credentials** — the Resource Owner authenticates with the Authorization Server.
4. **Client transfers credentials with OAuth keys** — the Client presents OAuth credentials to the Authorization Server.
5. **Authorization server issues Access Token** — a token is returned to the Client.
6. **Access Token transferred** — the Client presents the token to the Resource Server.
7. **Resource Server gives access** — access is granted based on the token's scope.

**Key components:** Resource Owner, Client, [[collection/entities/active-directory|Active Directory]] (implied credential store), Authorization Server, Resource Server.

## Key Distinctions Shown

| Dimension | SAML | OAuth |
|-----------|------|-------|
| Token format | XML assertion | Access token (opaque or JWT) |
| Primary use case | Enterprise SSO / federated identity | API authorization / delegated access |
| Credential store integration | Direct AD/LDAP lookup | Hidden behind Authorization Server |
| Primary actors | User ↔ IdP ↔ Service Provider | Resource Owner ↔ Client ↔ Auth Server ↔ Resource Server |

See [[collection/synthesis/saml-vs-oauth|SAML vs. OAuth — Comparison]] for cross-cutting analysis.
