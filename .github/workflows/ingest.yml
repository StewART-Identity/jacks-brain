name: Auto-ingest raw documents

on:
  workflow_dispatch:
    inputs:
      file_path:
        description: "Path to file in static/originals/ to ingest (leave blank for all new files)"
        required: false
        type: string
  push:
    branches: [main]
    paths: ["static/originals/**"]

permissions:
  contents: write

concurrency:
  group: ingest-main
  cancel-in-progress: false

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Install Claude Code
        run: npm install -g @anthropic-ai/claude-code

      - name: Install document converters
        run: sudo apt-get update && sudo apt-get install -y pandoc poppler-utils

      - name: Run ingest
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
        run: |
          if [ -n "${{ inputs.file_path }}" ]; then
            node scripts/ingest.mjs "${{ inputs.file_path }}"
          else
            node scripts/ingest.mjs
          fi

      - name: Commit and push to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add content/
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "ingest: auto-generated wiki pages from source documents"
            # Pull any commits that landed while we were processing, then push
            for i in 1 2 3 4; do
              git pull --rebase origin main && git push origin main && exit 0
              echo "Push attempt $i failed, retrying in $((i * 2))s..."
              sleep $((i * 2))
            done
            echo "Failed to push after 4 attempts"
            exit 1
          fi
